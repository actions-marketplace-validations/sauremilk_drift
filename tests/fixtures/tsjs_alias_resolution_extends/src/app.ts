import { feature } from "@leaf/feature";
import { util } from "@mid/util";
import { logger } from "@base/logger";
import { config } from "@shared/config";

console.log(feature, util, logger, config);
